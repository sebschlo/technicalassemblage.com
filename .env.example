# Optional. Buttondown newsletter username for the product "notify me" modal.
# This is your public Buttondown handle (buttondown.com/<username>), NOT an API key.
# Defaults to "technicalassemblage" in code, so you only need this to override it.
#
#   cp .env.example .env.local
VITE_BUTTONDOWN_USERNAME=
